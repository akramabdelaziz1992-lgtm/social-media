# نظام المكالمات الصوتية الحقيقية - الرقم الموحد

## 🎯 نظرة عامة

تم تطوير نظام مكالمات صوتية حقيقي باستخدام WebRTC يتيح إجراء مكالمات صوتية مباشرة من المتصفح.

## ✨ المميزات

### 1. مكالمات صوتية حقيقية
- استخدام WebRTC للاتصال المباشر peer-to-peer
- جودة صوت عالية مع:
  - Echo cancellation (إلغاء الصدى)
  - Noise suppression (تقليل الضوضاء)
  - Auto gain control (التحكم التلقائي بالصوت)

### 2. التحكم في المكالمة
- **كتم الصوت (Mute/Unmute)**: إيقاف/تشغيل الميكروفون
- **السماعة (Speaker)**: التحكم في مستوى الصوت
- **مدة المكالمة**: عداد زمني تلقائي
- **حالات المكالمة**:
  - `idle`: لا توجد مكالمة
  - `connecting`: جاري الاتصال
  - `ringing`: رنين
  - `active`: مكالمة نشطة
  - `ended`: انتهت المكالمة

### 3. واجهة مستخدم احترافية
- Banner نشط يظهر أثناء المكالمة
- مؤشرات بصرية لحالة المكالمة
- أزرار تحكم سهلة الاستخدام
- رسائل خطأ واضحة

## 🚀 كيفية الاستخدام

### بدء مكالمة جديدة

1. اضغط على زر **"اتصال جديد"**
2. أدخل رقم الهاتف (مثال: +966501234567)
3. اضغط **"اتصال"** أو Enter
4. سيطلب المتصفح إذن الوصول للميكروفون - اضغط **"السماح"**
5. ستبدأ المكالمة تلقائياً

### أثناء المكالمة

- **كتم الصوت**: اضغط على زر الميكروفون 🎤
- **تشغيل/إيقاف السماعة**: اضغط على زر السماعة 🔊
- **إنهاء المكالمة**: اضغط الزر الأحمر "إنهاء المكالمة"

### سجل المكالمات

- يتم حفظ كل مكالمة تلقائياً في السجل
- يمكنك مشاهدة:
  - مدة المكالمة
  - وقت وتاريخ المكالمة
  - الرقم المتصل به

## 🔧 التقنيات المستخدمة

### WebRTC (Web Real-Time Communication)
```typescript
- MediaDevices API: للوصول للميكروفون
- RTCPeerConnection: لإنشاء اتصال peer-to-peer
- ICE (Interactive Connectivity Establishment): لاكتشاف أفضل طريقة للاتصال
- STUN servers: من Google للمساعدة في NAT traversal
```

### React Hooks المخصصة
```typescript
useVoiceCall() - Hook شامل لإدارة المكالمات:
  - startCall(phoneNumber): بدء مكالمة
  - endCall(): إنهاء مكالمة
  - toggleMute(): كتم/إلغاء كتم الصوت
  - toggleSpeaker(): تشغيل/إيقاف السماعة
  - formattedDuration: مدة المكالمة بصيغة MM:SS
```

## 📝 ملاحظات مهمة

### الوضع الحالي (Development Mode)
النظام حالياً في وضع Demo ويعمل محلياً:
- ✅ يطلب إذن الميكروفون
- ✅ يقوم بإنشاء WebRTC connection
- ⚠️ لا يتصل فعلياً بأرقام خارجية (يحتاج backend)

### للتطبيق الكامل (Production)
لإجراء مكالمات حقيقية لأرقام هواتف خارجية، تحتاج:

#### 1. Backend Server مع VoIP Provider
خيارات مقترحة:
- **Twilio**: أشهر وأسهل خدمة VoIP
- **Vonage (Nexmo)**: بديل ممتاز
- **Plivo**: خيار اقتصادي
- **AWS Chime SDK**: للمؤسسات الكبيرة

#### 2. Signaling Server
```javascript
// مثال بسيط باستخدام Socket.IO
io.on('connection', (socket) => {
  socket.on('call-offer', (data) => {
    socket.to(data.to).emit('call-offer', data);
  });
  
  socket.on('call-answer', (data) => {
    socket.to(data.to).emit('call-answer', data);
  });
  
  socket.on('ice-candidate', (data) => {
    socket.to(data.to).emit('ice-candidate', data);
  });
});
```

#### 3. تكامل مع Twilio (مثال)
```typescript
// Backend endpoint
app.post('/api/calls/start', async (req, res) => {
  const { to, from } = req.body;
  
  const call = await twilioClient.calls.create({
    to: to,
    from: process.env.TWILIO_PHONE_NUMBER,
    twiml: `<Response><Dial>${to}</Dial></Response>`
  });
  
  res.json({ callSid: call.sid });
});
```

## 🔐 الأمان والخصوصية

### إذن الميكروفون
- يطلب المتصفح إذن المستخدم قبل الوصول للميكروفون
- الإذن يُحفظ لكل موقع على حدة
- يمكن إلغاء الإذن من إعدادات المتصفح

### تشفير المكالمات
- WebRTC يستخدم DTLS-SRTP للتشفير تلقائياً
- كل المكالمات مشفرة end-to-end
- لا يمكن التنصت على المكالمات

### متطلبات HTTPS
- WebRTC يتطلب HTTPS في production
- localhost معفى من هذا الشرط (للتطوير)

## 🐛 حل المشاكل الشائعة

### المتصفح لا يطلب إذن الميكروفون
```
الحل:
1. تأكد من استخدام HTTPS (أو localhost)
2. تحقق من إعدادات الميكروفون في النظام
3. جرّب متصفح آخر (Chrome/Edge موصى بهما)
```

### "Permission Denied" للميكروفون
```
الحل:
1. افتح إعدادات الموقع في المتصفح
2. اسمح بالوصول للميكروفون
3. أعد تحميل الصفحة
```

### لا يوجد صوت
```
الحل:
1. تحقق من عدم كتم الصوت (Mute)
2. تأكد من تشغيل السماعة
3. جرّب سماعات/مكبرات صوت مختلفة
4. تحقق من إعدادات الصوت في النظام
```

### جودة صوت رديئة
```
الحل:
1. تحقق من سرعة الإنترنت
2. أغلق البرامج الأخرى التي تستخدم الميكروفون
3. استخدم سماعة رأس بدلاً من المكبرات (لتجنب الصدى)
```

## 📊 التطوير المستقبلي

### Phase 1 (حالياً) ✅
- [x] WebRTC setup
- [x] واجهة المستخدم
- [x] التحكم بالمكالمة
- [x] سجل المكالمات

### Phase 2 (قريباً)
- [ ] Backend integration
- [ ] Twilio/VoIP provider
- [ ] تسجيل المكالمات
- [ ] مكالمات واردة

### Phase 3 (مستقبلاً)
- [ ] مكالمات فيديو
- [ ] Conference calls
- [ ] Call forwarding
- [ ] Voicemail
- [ ] IVR (Interactive Voice Response)
- [ ] Analytics ومتابعة الأداء

## 🌐 المتصفحات المدعومة

| المتصفح | الدعم | ملاحظات |
|---------|-------|----------|
| Chrome | ✅ ممتاز | موصى به |
| Edge | ✅ ممتاز | موصى به |
| Firefox | ✅ جيد | بعض القيود |
| Safari | ⚠️ محدود | يحتاج iOS 14.3+ |
| Opera | ✅ جيد | - |

## 📞 الدعم الفني

للأسئلة أو المشاكل:
1. تحقق من console المتصفح (F12)
2. راجع هذا الملف
3. اتصل بفريق التطوير

## 📚 مصادر إضافية

- [WebRTC Documentation](https://webrtc.org/)
- [MDN WebRTC Guide](https://developer.mozilla.org/en-US/docs/Web/API/WebRTC_API)
- [Twilio Voice SDK](https://www.twilio.com/docs/voice)
- [STUN/TURN Servers](https://gist.github.com/sagivo/3a4b2f2c7ac6e1b5267c2f1f59ac6c6b)

---

💡 **نصيحة**: للحصول على أفضل تجربة، استخدم سماعة رأس جيدة واتصال إنترنت مستقر!
