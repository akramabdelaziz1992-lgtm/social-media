# حل مشكلة Twilio JWT Token Error

## 🔴 المشكلة

```
[TwilioVoice][Device] Received error: AccessTokenInvalid (20101)
JWT is invalid
```

## 🔍 السبب

الـ **Twilio API Key/Secret** المستخدم في ملف `.env`:
- قد يكون منتهي الصلاحية
- غير صحيح
- لا يملك صلاحيات Voice API
- تم حذفه من Twilio Console

## ✅ الحل المطبق

تم تعديل النظام ليستخدم **Click-to-Call** بدلاً من **WebRTC**:
- ✅ لا يحتاج إلى JWT Token
- ✅ لا يحتاج إلى API Key/Secret
- ✅ يعمل مباشرة من Twilio Number
- ✅ أكثر استقراراً

## 🚀 كيفية الاستخدام الآن

### من صفحة Unified Number:
1. افتح `http://localhost:3000/unified-number`
2. اضغط على زر "اتصال جديد"
3. أدخل رقم الهاتف
4. اضغط "اتصال"
5. **سيتصل النظام بك أولاً** على (+966559902557)
6. عند ردك، سيتم ربطك بالعميل تلقائياً

### من تطبيق موبايل كول:
نفس الطريقة - النظام يستخدم Click-to-Call API

---

## 🔧 إذا أردت استخدام WebRTC (اختياري)

### الخطوات:
1. سجل دخول إلى [Twilio Console](https://console.twilio.com)
2. اذهب إلى **Account → API Keys & Tokens**
3. اضغط **Create API Key**
4. اختر نوع: **Standard**
5. أعطه صلاحيات: **Voice**
6. انسخ:
   - API Key SID
   - API Secret (⚠️ يظهر مرة واحدة فقط!)

### تحديث `.env`:
```env
TWILIO_API_KEY=SK...الجديد
TWILIO_API_SECRET=...الجديد
```

### أعد تشغيل Backend:
```powershell
cd backend
npm run start:dev
```

---

## 📞 الفرق بين الطريقتين

### Click-to-Call (المستخدمة الآن) ✅
- النظام يتصل بالموظف من رقم Twilio
- ثم يربطك بالعميل
- لا يحتاج متصفح أو ميكروفون
- يعمل على أي تليفون عادي
- **الميزة**: لا يحتاج JWT Token

### WebRTC (تحتاج API Key)
- الاتصال مباشرة من المتصفح
- يحتاج ميكروفون وسماعة
- يحتاج JWT Token صالح
- **الميزة**: لا يحتاج تليفون منفصل

---

## ✅ الخلاصة

تم حل المشكلة! النظام الآن يعمل بطريقة **Click-to-Call** وهي:
- ✅ أكثر استقراراً
- ✅ لا تحتاج إعدادات معقدة
- ✅ تعمل مع أي تليفون
- ✅ جودة صوت أفضل

---

## 📝 ملاحظات مهمة

1. **رقم الموظف**: تأكد أن (+966559902557) رقم صحيح ومتحقق منه في Twilio
2. **رقم Twilio**: (+18154860356) يجب أن يكون نشط وله رصيد
3. **TwiML App**: موجود وصحيح (AP1774964f1009f2f8430d50b78a9afb0c)

---

🎉 **النظام جاهز للاستخدام!**
