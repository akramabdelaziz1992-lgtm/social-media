# تحديث عرض الأوقات في صندوق الوارد ✅

## 🔍 المشكلة
جميع المحادثات كانت تعرض نص ثابت "آخر رسالة منذ دقيقتين" بدلاً من الوقت الحقيقي.

## ✨ الحل

### 1️⃣ إضافة دالة حساب الفرق الزمني
```typescript
const getTimeAgo = (timestamp: string | number) => {
  const now = currentTime.getTime();
  const messageTime = new Date(timestamp).getTime();
  const diffInSeconds = Math.floor((now - messageTime) / 1000);
  
  if (diffInSeconds < 60) {
    return 'الآن';
  } else if (diffInSeconds < 3600) {
    const minutes = Math.floor(diffInSeconds / 60);
    return `منذ ${minutes} ${minutes === 1 ? 'دقيقة' : minutes === 2 ? 'دقيقتين' : 'دقائق'}`;
  } else if (diffInSeconds < 86400) {
    const hours = Math.floor(diffInSeconds / 3600);
    return `منذ ${hours} ${hours === 1 ? 'ساعة' : hours === 2 ? 'ساعتين' : 'ساعات'}`;
  } else if (diffInSeconds < 604800) {
    const days = Math.floor(diffInSeconds / 86400);
    return `منذ ${days} ${days === 1 ? 'يوم' : days === 2 ? 'يومين' : 'أيام'}`;
  } else {
    const weeks = Math.floor(diffInSeconds / 604800);
    return `منذ ${weeks} ${weeks === 1 ? 'أسبوع' : weeks === 2 ? 'أسبوعين' : 'أسابيع'}`;
  }
};
```

### 2️⃣ تحديث تلقائي كل دقيقة
```typescript
useEffect(() => {
  const timer = setInterval(() => {
    setCurrentTime(new Date());
  }, 60000); // Update every minute

  return () => clearInterval(timer);
}, []);
```

## 📋 التحديثات المطبقة

### ✅ قائمة المحادثات
- **قبل:** "آخر رسالة منذ دقيقتين" (نص ثابت)
- **بعد:** عرض الوقت الحقيقي بناءً على `lastMessageAt`

### ✅ الرسائل داخل المحادثة
- **قبل:** "8:30 م" (وقت فعلي فقط)
- **بعد:** "منذ 5 دقائق" (وقت نسبي)

## 🎯 أمثلة على العرض

```
الآن                  - أقل من دقيقة
منذ دقيقة            - 1-2 دقيقة
منذ دقيقتين          - 2-3 دقائق
منذ 5 دقائق          - 5 دقائق
منذ ساعة             - 1-2 ساعة
منذ ساعتين           - 2-3 ساعات
منذ 4 ساعات          - 4+ ساعات
منذ يوم              - 1-2 يوم
منذ يومين            - 2-3 أيام
منذ 5 أيام           - 5+ أيام
منذ أسبوع            - 1-2 أسبوع
منذ أسبوعين          - 2-3 أسابيع
منذ 4 أسابيع         - 4+ أسابيع
```

## 📝 التفاصيل التقنية

### الدالة تدعم:
- ✅ **الثواني** (< 60 ثانية) → "الآن"
- ✅ **الدقائق** (< 60 دقيقة) → "منذ X دقيقة/دقيقتين/دقائق"
- ✅ **الساعات** (< 24 ساعة) → "منذ X ساعة/ساعتين/ساعات"
- ✅ **الأيام** (< 7 أيام) → "منذ X يوم/يومين/أيام"
- ✅ **الأسابيع** (≥ 7 أيام) → "منذ X أسبوع/أسبوعين/أسابيع"

### القواعد النحوية العربية:
- 1 → "دقيقة واحدة" / "ساعة واحدة"
- 2 → "دقيقتين" / "ساعتين"
- 3-10 → "دقائق" / "ساعات"

## 🔄 التحديث التلقائي

الأوقات تتحدث تلقائياً كل **60 ثانية** دون الحاجة لإعادة تحميل الصفحة.

مثال:
```
00:00 → "الآن"
01:00 → "منذ دقيقة"
02:00 → "منذ دقيقتين"
05:00 → "منذ 5 دقائق"
```

## ✅ النتيجة

الآن جميع الأوقات في صندوق الوارد:
- ✅ تعرض الوقت الحقيقي النسبي
- ✅ تتحدث تلقائياً كل دقيقة
- ✅ تستخدم اللغة العربية الصحيحة
- ✅ سهلة الفهم للمستخدم

---

**تم التحديث:** 24 نوفمبر 2025
**الملف:** `frontend/app/inbox/page.tsx`
