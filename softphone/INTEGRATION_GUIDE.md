# ربط تطبيق موبايل كول مع النظام الرئيسي

## التحديثات المنفذة 🚀

### 1️⃣ تطبيق موبايل كول (Softphone)

#### التعديلات في `main.js`:
- ✅ إضافة IPC handler `show-window` لفتح التطبيق من خارجه
- ✅ إضافة IPC handler `quit-app` لإغلاق التطبيق بشكل صحيح

#### التعديلات في `renderer.js`:
- ✅ تحديث دالة `makeCall()` للاتصال بـ Backend API وحفظ المكالمة
- ✅ تحديث دالة `endCall()` لإرسال حالة المكالمة للـ Backend
- ✅ حفظ `callSid` من Twilio لتتبع المكالمات
- ✅ إرسال أحداث المكالمات عبر WebSocket

### 2️⃣ صفحة Unified Number

#### التحديثات في `app/unified-number/page.tsx`:
- ✅ إضافة زر "موبايل كول" لفتح التطبيق
- ✅ إضافة دالة `openSoftphone()` لفتح التطبيق
- ✅ إضافة دالة `loadCallRecords()` لجلب المكالمات من Backend
- ✅ عرض المكالمات الحقيقية من قاعدة البيانات
- ✅ تحديث تلقائي كل 30 ثانية

### 3️⃣ Backend API

المكالمات تُحفظ تلقائياً في جدول `calls` عبر:
- `POST /api/calls/make-call` - بدء مكالمة جديدة
- `POST /api/calls/webhook/status` - تحديث حالة المكالمة
- `GET /api/calls` - جلب جميع المكالمات

## كيفية الاستخدام 📱

### الطريقة 1: من صفحة Unified Number
1. افتح المتصفح على `http://localhost:3000/unified-number`
2. اضغط على زر "موبايل كول" 💙 (الأزرق بجانب زر "اتصال جديد")
3. سيتم فتح تطبيق موبايل كول تلقائياً

### الطريقة 2: تشغيل التطبيق مباشرة
```powershell
cd "d:\social media\almasar-suite\softphone"
npm start
```

### إجراء مكالمة
1. أدخل رقم الهاتف في تطبيق موبايل كول
2. اضغط على زر الاتصال ☎️
3. سيتم:
   - الاتصال بالموظف أولاً (+966559902557)
   - عند رد الموظف، يتم الاتصال بالعميل
   - تسجيل المكالمة تلقائياً
   - حفظ المكالمة في قاعدة البيانات

### عرض سجل المكالمات
1. افتح `http://localhost:3000/unified-number`
2. اذهب إلى تبويب "سجل المكالمات"
3. سترى جميع المكالمات من:
   - تطبيق موبايل كول ✅
   - المكالمات الواردة من Twilio ✅
   - المكالمات من الواجهة الرئيسية ✅

## البيانات المحفوظة 💾

كل مكالمة تحتوي على:
- `id` - معرف المكالمة
- `twilioCallSid` - معرف المكالمة في Twilio
- `fromNumber` - الرقم المتصل
- `toNumber` - الرقم المستقبل
- `direction` - الاتجاه (inbound/outbound)
- `status` - الحالة (initiated/ringing/in-progress/completed/failed)
- `durationSeconds` - مدة المكالمة بالثواني
- `recordingUrl` - رابط تسجيل المكالمة
- `agentId` - معرف الموظف
- `agentName` - اسم الموظف
- `createdAt` - تاريخ البدء
- `endedAt` - تاريخ الانتهاء

## الأرقام المستخدمة 📞

- **رقم Twilio**: +18154860356
- **رقم الموظف**: +966559902557
- **رقم مركز الاتصالات**: 0555254915

## التسجيل التلقائي 🎙️

جميع المكالمات يتم تسجيلها تلقائياً من لحظة رد العميل:
- الجودة: HD
- التخزين: Twilio Cloud
- المدة: حسب إعدادات Twilio
- يمكن الاستماع للتسجيل من صفحة unified-number

## الإعدادات ⚙️

يمكن تعديل الإعدادات في تطبيق موبايل كول:
- **عنوان الخادم**: http://localhost:4000 (افتراضي)
- **الاتصال التلقائي**: مفعّل
- **الإشعارات**: مفعّلة

## استكشاف الأخطاء 🔧

### المشكلة: التطبيق لا يفتح
**الحل**: تأكد من تشغيل التطبيق مرة واحدة يدوياً أولاً
```powershell
cd softphone
npm start
```

### المشكلة: المكالمات لا تُحفظ
**الحل**: تحقق من اتصال Backend
```powershell
curl http://localhost:4000/api/calls
```

### المشكلة: لا يوجد اتصال بالخادم
**الحل**: تحقق من إعدادات التطبيق (تبويب Settings)

## الميزات المستقبلية 🎯

- [ ] إضافة دعم لعدة موظفين
- [ ] نظام الطوابير (Queue)
- [ ] الاتصال عبر WebRTC مباشرة
- [ ] تكامل مع WhatsApp Business
- [ ] إشعارات سطح المكتب للمكالمات الواردة
- [ ] تقارير تفصيلية لأداء الموظفين

## المتطلبات 📋

- Node.js 16+
- Electron 28+
- Backend API يعمل على port 4000
- Frontend يعمل على port 3000
- حساب Twilio نشط

## الدعم والمساعدة 💬

للإبلاغ عن مشاكل أو اقتراحات:
1. تحقق من ملف `BOT_SYSTEM_README.md`
2. راجع `COMPLETION_STATUS.md`
3. اطلع على `START_HERE.md`

---

✅ **تم إكمال الربط بنجاح!**
